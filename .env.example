# Database Configuration
DATABASE_URL=postgresql+asyncpg://username:password@localhost:5432/prdsherpa

# ===========================================
# AI 模型配置 (Multi-Model Support)
# ===========================================

# 1️⃣ Google Gemini（必需，默认使用，免费）
# 获取密钥: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-3-flash-preview

# 2️⃣ OpenAI GPT-4（可选，付费）
# 获取密钥: https://platform.openai.com/api-keys
# 如不使用，保持为空即可
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4-turbo-preview

# 3️⃣ Anthropic Claude（可选，付费）
# 获取密钥: https://console.anthropic.com/
# 如不使用，保持为空即可
CLAUDE_API_KEY=
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# 默认使用的 AI 提供商（gemini/openai/claude）
DEFAULT_AI_PROVIDER=gemini

# 🌐 网络代理配置（可选，国内用户推荐配置）
# 如果需要使用代理访问 AI 服务，请填写代理地址
# Clash/V2ray 常见端口: 7890, Shadowsocks: 1087
# 示例: http://127.0.0.1:7890
HTTP_PROXY=
HTTPS_PROXY=

# Redis Configuration (for Celery)
REDIS_URL=redis://localhost:6379/0

# File Upload Configuration
UPLOAD_DIR=./uploads
MAX_FILE_SIZE_MB=10
MAX_FILES_PER_PROJECT=50

# Application Configuration
DEBUG=True
SECRET_KEY=your_secret_key_here_change_in_production
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Celery Configuration
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
